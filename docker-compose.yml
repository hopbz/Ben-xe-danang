services:

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: quanlyxe
  #     POSTGRES_USER: ${DB_USERNAME:-postgres}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres123}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   networks:
  #     - quanlyxe-network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres} -d quanlyxe"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  backend:
    build:
      context: ./BE
      dockerfile: Dockerfile
    container_name: quanlyxe-be
    ports:
      - "8080:8080"

    environment:
      - SPRING_PROFILES_ACTIVE=postgresql
      - SERVER_PORT=8080
      - ALLOWED_ORIGINS=http://localhost:3000
      # Cấu hình kết nối tới Render:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-d7i6i09f9bms73fvr7p0-a.singapore-postgres.render.com/quanlyxe_jr7a?sslmode=require
      - SPRING_DATASOURCE_USERNAME=post_gres
      - SPRING_DATASOURCE_PASSWORD=rsSCDrJr5OFgX7h6eHc8BVqQ1xouqDIq
    networks:
      - quanlyxe-network

  frontend:
    build:
      context: ./FE
      dockerfile: Dockerfile
    container_name: quanlyxe-fe
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - quanlyxe-network

networks:
  quanlyxe-network:
    driver: bridge

# volumes:
#   pgdata: